load 'lib/socket.rb'

namespace :client do
  desc "create websocket connection until server terminates it"
  task :websocket_request do

    puts 'Creating websocket request ...'

    Socket.run
  end
end